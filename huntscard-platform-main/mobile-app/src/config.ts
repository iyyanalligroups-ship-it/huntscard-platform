// Points the app at your HuntsTAG backend. Change this to your real
// domain (https://huntstag.com) once you're testing against production
// instead of a local dev server.
//
// NOTE: "localhost" means the PHONE, not your computer -- a physical
// phone can never reach your PC's localhost. This uses the PC's
// Ethernet static IP (fixed, doesn't change on reconnect/reboot like
// the WiFi adapter's IP did) so this file shouldn't need updating again
// for local testing -- only your phone still needs to be on the same
// home WiFi network as this PC for the LAN route to work.
export const BACKEND_URL = 'http://192.168.1.33:4000';
